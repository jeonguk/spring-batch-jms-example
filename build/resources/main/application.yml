logging:
  pattern:
    console: "%msg%n"
  level:
    org.springframework: info
    com.jeonguk: info

spring:
  activemq:
    broker-url: vm://localhost
  jms:
    template:
      default-destination: person-queue
      receive-timeout: 2s